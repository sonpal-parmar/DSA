#include <iostream>
using namespace std;

int prime(int a[], int n)
{
    int f, i, j, c = 0;
    i = 0;
    while (i < n)
    {
        j = 2;
        f = 0;
        while (j < a[i])
        {
            if (a[i] % j == 0)
            {
                f = 1
            }
        }
    }
}